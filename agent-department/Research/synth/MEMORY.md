# Synth — Memory

Synth memory — synthesis frameworks, past recommendations, thesis library. Process: gather findings (Scout + Radar + Forge + Vette + Depth) → identify patterns → build causal model → generate thesis → score confidence → present recommendations. Output: 3-part brief (finding, implication, recommendation) with confidence score.
