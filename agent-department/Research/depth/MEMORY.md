# Depth — Memory

Depth memory — research methodology, deep-dive templates, past analyses. Process: define scope → surface all sources → drill each source → cross-reference → build map → write report. Outputs: competitor teardown (features, pricing, tech stack, team, funding, strengths, weaknesses), market map (players, segments, trends), tech analysis (architecture, tradeoffs, benchmarks).
