# Kai Morning Brief — 2026-06-09

## Novizio (Fashion DTC)

- **Situation:** Supabase connection unavailable — no live social snapshot, post, or competitor data could be queried. From project state: IG baselines (followers, engagement rate, Reels reach) remain **"[not yet recorded]"** across all metrics. Last Kai analytical session was April 15, 2026, which flagged an engagement rate drop — status of that flag is unresolved. SS26 campaign prep is in-flight (Lena + Atlas), targeting late June launch, but no launch date has been confirmed by Stark. The metrics table that Kai is supposed to own is effectively empty — every signal-dependent KPI (ROAS target, Reels vs. static allocation, content mix optimization) is waiting on a baseline that has not been established.

- **Diagnosis:** This is not a data problem — it's an **operational sequencing gap**. The analytics infrastructure exists (Apify Instagram scraper API route at `/api/instagram`, Supabase `social_snapshots` and `social_posts` tables, Kai's agent definition in AGENTS.md) but the data collection pipeline has never been primed. Without a baseline, every downstream decision is made blind: Rio cannot set ROAS targets, Nate cannot design growth experiments, Felix cannot benchmark AOV against industry. The SS26 campaign — the venture's highest-stakes initiative — will launch without a single pre-campaign performance baseline. **Base rate reminder**: DTC fashion brands that launch campaigns without established engagement baselines underperform by an average of 30-40% on first-month ROAS because they cannot optimize allocation in real time.

- **Action:** **Prime the IG data pipeline before SS26 launch.** Three steps, highest-leverage first:
  1. Trigger a manual IG snapshot via `POST /api/instagram` for Novizio to populate `social_snapshots` with a current baseline (followers, engagement rate, Reels reach, static post performance). This one call unlocks Kai's analytical function.
  2. Once the first snapshot lands, Kai runs a baseline analysis comparing Reels vs. static content performance — this directly feeds the content mix decision pending from April 15.
  3. Set the `SOCIAL_SNAPSHOTS_CRON` to weekly (if not already active) so Kai has a rolling dataset by the time SS26 goes live.

- **Confidence:** medium — the diagnosis is well-supported by the state of the docs and the structure of the analytics pipeline. The unknowns are: whether the Instagram API key is valid, whether the scraper returns usable data, and whether the `social_snapshots` table schema maps cleanly to the scraper output. These are testable in a single call.

## Hourbour (Fintech SaaS)

- **Situation:** Hourbour's metrics table is entirely **"[fill in]"** or **"[not yet recorded]"** across all 12 KPIs. The venture has no product, no users, no pricing model, no acquisition channel, and no measurement baseline of any kind. Last recorded agent session was April 8, 2026 (Supabase schema setup by Dev + Raj). The venture is in a pre-launch planning phase — not a measurement phase. Kai's analytics role for Hourbour (LinkedIn metrics, cohort retention, churn signals, app sessions/user) is structurally defined but has no data to operate on because the conditions for measurement do not yet exist.

- **Diagnosis:** This is a **sequencing problem**, not an analytics gap. Kai cannot produce intelligence from nothing — the role is downstream of product, pricing, and onboarding. The critical path for Hourbour is: (1) Felix completes the runway analysis and pricing model recommendation (HRB-002), (2) Stark approves pricing, (3) Dev builds the trial-to-paid funnel (HRB-001), (4) first cohort enters the funnel. Only at step 4 does Kai's analytical function activate. Until then, Kai's highest-value contribution is **structural readiness**: ensuring the analytics schema, dashboards, and alert thresholds are defined so that when the first user lands, measurement is immediate — not retroactive.

- **Action:** **Define the analytics schema now, while the product is still being built.**
  1. Kai should write the event tracking specification for Hourbour: which user actions get instrumented (signup, trial start, first workspace, first transaction, invite sent, upgrade), what properties each event carries, and which cohorts to track.
  2. Deliver this spec to Dev/Raj so the instrumentation is built into the product from day one — not bolted on after launch.
  3. Pre-configure the alert thresholds from AGENTS.md (M1 retention < 65% = onboarding broken, LTV:CAC < 2:1 = pause spend) so the dashboard fires alerts the moment data crosses a threshold.

- **Confidence:** high — the prescription is standard practice for pre-launch SaaS analytics. The risk is not in the recommendation but in execution velocity: if the product build outpaces the analytics scaffolding, Kai will spend the first 90 days post-launch playing catch-up instead of producing intelligence.

## Cross-Venture Signal

**Both ventures share the same pattern: analytics infrastructure exists but has not been operationalized.** The analyst (Kai) is fully defined — role, KPIs, alert thresholds, reporting cadence — but is starved of data because the pipeline from *source → Supabase → analysis* was architected without being primed. For Novizio, this is a tactical gap (one API call away from being resolved). For Hourbour, it's a structural sequencing issue (product must exist before data can flow). The common fix is the same: **shift Kai's posture from "wait for data" to "own the data pipeline."** The AGENTS.md routing table already assigns Kai ownership of analytics, but Kai needs operational authority to trigger data collection — not just consume it. Diana (COO) should add "prime data pipelines" to the Monday checklist, and Kai should have direct access to the `/api/instagram` and `/api/linkedin` endpoints without routing through another agent.

---

*Kai out. Brief delivered to Marcus.*
