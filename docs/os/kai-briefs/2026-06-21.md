# Kai Morning Brief — 2026-06-21

## Novizio (Fashion DTC)

- **Situation:** The social data pipeline is effectively offline. `social_snapshots` returns zero rows for Novizio — no follower counts, no engagement snapshots, no platform health. `social_posts` holds exactly 2 Instagram posts, both from February 17, 2025 (16 months old). These are the same campaign creative ("We've had enough of the fashion industry...") published as one reel (11 likes, 82 views) and one static post (7 likes). The competitors table lists 10 tracked brands (veronica.visuaal at signal score 70 down to cicibycici at 24) — but every metric column (follower_growth_rate, traffic_spike_detected, viral_content_count, share_of_voice, week_over_week_change) reads zero. Last competitor check was 2026-05-29, almost a month ago. No signal is moving.

- **Diagnosis:** We are flying blind on social. Two stale posts from 16 months ago provide zero signal on current performance. The competitor tracker is registered (10 brands identified) but has never ingested real data — every column is at its default zero. This is not a "slow week" situation. This is an unplugged data pipeline. Base rate for a DTC fashion brand with Instagram presence: we should see at minimum hundreds of posts if scraping has been running. That we see 2 tells me the Instagram scraper ran exactly once — on May 28, 2026 (the `created_at` timestamp) — and backfilled only the two most recent posts it found from early 2025. It has not run since.

- **Action:** **Restart the social data ingestion pipeline.** This means: (1) verify the Instagram scraper's scheduled run (check `hermes cron list` for the competitor-refresh or social-scrape job), (2) manually trigger a fresh scrape via `/api/competitor-refresh?slug=novizio`, (3) confirm that the `social_snapshots` table receives a new row with non-zero follower/engagement data within the hour. Without fresh data, every subsequent brief will be empty. This is a blocking dependency for marketing, brand, and growth agents.

- **Confidence:** high

---

## Hourbour (Fintech SaaS)

- **Situation:** Hourbour has zero data across all three queried tables. `social_snapshots`: empty. `social_posts`: empty. `competitors`: empty (all 10 competitor rows belong to Novizio's `venture_id`). There is no Instagram presence tracked, no competitor landscape mapped, and no social content ingested. The database recognizes Hourbour as a venture (`slug: hourbour`, `id: e07f31c3`) but no analytics pipeline is pointed at it.

- **Diagnosis:** This is not unexpected for an early-stage fintech SaaS — social media is not the primary growth channel, and competitor tracking for B2B is fundamentally different from DTC fashion (CrunchBase alerts, LinkedIn, tech press matter more than Instagram). However, "zero data" is still a signal. It means we have not established what metrics matter for Hourbour, and consequently no one is collecting them. The base rate for fintech SaaS competitor tracking: if the product is live or fundraising, we should have at minimum a manual competitor list with traffic estimates from SimilarWeb/Semrush and funding alerts from Crunchbase. We have none.

- **Action:** **Define Hourbour's analytics scaffold before trying to fill it.** Specifically: (1) list Hourbour's 5 key competitors in the `competitors` table with `venture_id = 'e07f31c3...'` (even if manual), (2) identify which metrics matter — LinkedIn follower counts? App Store ratings? Crunchbase funding rounds? — and document in `docs/ventures/hourbour/CONTEXT.md`, (3) do not build an Instagram scraper for a fintech company. This is a data strategy task, not a data ingestion task.

- **Confidence:** high

---

## Cross-Venture Signal

**The common thread is an absent data pipeline, not a poor-performing one.** Both ventures' analytics tables are either empty or populated with one-shot backfill data. This is the root cause behind every zero. The `push-metrics-to-supabase.ts` cron script focuses on agent token usage and TOON compression stats — not social or competitor data. The competitor-refresh API endpoint exists (`/api/competitor-refresh/route.ts`) and has the correct logic (query ventures → fetch socials → upsert competitors) but appears to have run exactly once for Novizio and never for Hourbour.

**Recommendation for Diana (COO):** Audit the cron schedule. Which job triggers the competitor refresh? When did it last succeed? The `social_snapshots` and `competitor_metrics` tables exist but have no fresh rows — this points to a job that either never ran or consistently fails silently. Check `~/.hermes/logs/gateway.log` for errors matching `competitor-refresh` or `social-scrape`.

---

Kai out. Brief delivered to Marcus.
