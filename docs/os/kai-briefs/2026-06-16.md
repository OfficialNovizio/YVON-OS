# Kai Morning Brief — 2026-06-16

## Novizio (Fashion DTC)

- **Situation:** Intelligence pipeline is dark. The `social_snapshots` table is empty — zero snapshots collected. `social_posts` contains only 2 Instagram entries from **February 2025** (16 months ago), both with sub-12 likes and ~0.1% engagement. The `competitors` table has 10 tracked brands (veronica.visuaal leads at signal score 70, atelier_pozdniakova at 67), but all competitor metrics are zeroed — no follower growth, no viral content detection, no share-of-voice data. Last competitor scan was **18 days ago** (May 29, 2026). GA4 property (G-50FKHF7V8J) is configured but connectivity is unverified from this query path.

- **Diagnosis:** This is not a data problem — it's an **infrastructure gap**. The social listening pipeline is either not running or not connected to Supabase. The `social_snapshots` table structure exists but has never received a row. The 2 posts that do exist were backfilled on 2026-05-28 (3 weeks ago) — likely a one-time import, not an ongoing feed. No competitor refresh has happened in 18 days. We are flying blind on a venture that operates in fast-fashion (target demo: women 15-45, India market, INR 1,200 average price point) where competitor velocity and social sentiment change weekly.

- **Action:** **Connect and trigger the social listening pipeline.** The highest-leverage single move is to verify the `scripts/push-metrics-to-supabase.ts` script can reach Instagram's API and write to `social_snapshots`, then run a competitor refresh cycle. Without fresh data, every decision from Marcus down is base-rate guessing. If the pipeline requires API keys or tokens that have expired, that's priority-one to resolve.

- **Confidence:** high (on the diagnosis — the data speaks clearly: tables exist, data doesn't)

---

## Hourbour (Fintech SaaS)

- **Situation:** Worse than Novizio. **Zero** social posts. **Zero** competitor tracking. **Zero** snapshots. The `competitors` table has 10 rows but all are mapped to Novizio's venture_id — Hourbour doesn't have a single competitor entry. The `ventures` table confirms Hourbour exists (slug: hourbour, created 2026-03-22, repo: OfficialNovizio/Hourbour) but the analytics layer has never been populated for this venture. The only Hourbour-related activity in the database comes from `agent_sessions` — Marcus's CEO assessment on May 23 concluded: "Hourbour is a brand on paper, not in market."

- **Diagnosis:** Hourbour is in **pre-launch / brand-definition phase**. It's not generating market data because it has no market presence yet. Marcus's assessment from 3 weeks ago flagged: no product code, no pricing model, no acquisition channel, no trial-to-paid flow. The intelligence gap is secondary to the **existence gap** — you can't measure what isn't in market. The question isn't "what's the trend?" but "are we building anything to eventually measure?"

- **Action:** **Define Hourbour's competitive set and wire the tracking.** Even pre-launch, competitor monitoring should be active — identify 5-10 fintech SaaS competitors (Canadian market, given operating country: CA), add them to the `competitors` table with `venture_id = e07f31c3-c262-4796-ab81-d457b91dc954`, and set up weekly signal checks. This costs nothing and ensures that when Hourbour does launch, we have a baseline.

- **Confidence:** high (on the data gap), medium (on the go-to-market readiness — inferred from Marcus's session, not direct market signals)

---

## Cross-Venture Signal

**The signal is the absence of signal.** Both ventures share the same root cause: the YVON intelligence infrastructure exists on paper (tables, scripts, API routes) but is **not operational**. The `social_snapshots` collector has never run. Competitor tracking is stale. Social post ingestion stopped 16 months ago. This isn't a Novizio problem or an Hourbour problem — it's a **platform problem**.

**Systemic risk:** If Marcus or Diana are making venture decisions based on dashboard data, they are looking at empty or stale tables. The entire "intelligence → decision" loop is broken at step one.

**Recommended cross-venture action:** A one-hour "intelligence pipeline audit" — verify each data source (Instagram API, GA4, competitor scrapers), confirm API keys are valid, run a full refresh cycle, and set up the cron job that keeps it running. The `scripts/push-metrics-to-supabase.ts` script should be the starting point. If it doesn't work, fix it. If it does, schedule it.

---

Kai out. Brief delivered to Marcus.
