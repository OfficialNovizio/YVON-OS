# Dependencies
node_modules/

# Next.js
.next/
out/

# Environment variables — never commit real keys
.env*.local
.env

# Agent proposals — operational data, not code
requests/pending/*
requests/approved/*
requests/rejected/*
!requests/README.md
!requests/pending/EXAMPLE.md

# Build outputs
dist/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp

# Claude Code local settings
.claude/settings.local.json
.claude/mcps/
.vercel

# Hermes runtime clone (aux-tools sidecar) — large, fetched via npm run hermes:setup
hermes/vendor/

# TOON build artifacts — auto-generated from .md sources (npm install / postinstall)
.toon/

# Raw agent/memory/graph dirs — migrated to .toon/ (TOON-compressed)
# These are deployment artifacts from ToonGine, not source of truth
agent-department/
agent-memory/
graphify-out/
