# Forge — Memory

Forge memory — methods discovered, benchmarks run, upgrade proposals. Current stack: DeepSeek v4 (primary), ToonGine v4 (compression), TOON format (context). Search targets: new compression methods, prompt optimization, agent architectures, multi-agent coordination patterns. Evaluation: benchmark → compare → recommend if >10% improvement.
