#DOC id=ROADMAP source=/root/yvon/docs/os/ROADMAP.md format=toon-doc compressed=2026-06-12T06:40:26.317Z

S|# YVON Master Roadmap|> Created: 2026-04-01 \| Owner: Stark \| rv: weekly every Monday // > DRI for execution: Stark. Each item must reach "Shipped" state before the next Priority tier begins. // ---
S|Priority 1 — Foundation (everything else depends on this)|### P1-A: Brand Sidebar + vtr Scoping `Point 4` // - Brands listed in left sidebar as clickable cards (Novizio, Hourbour) // - Click brand → writes `yvon_active_venture` cookie → all pages re-render scoped to that brand // - Each dashboard view is brand-isolated: no Novizio data bleeds into Hourbour views // - DRI: Mia (UI) + Raj (cookie/API) \| Done when: clicking a brand in sidebar scopes the entire app // - **Status:** Scoped // ### P1-B: Project mem + Selective Loading `Point 12` // - Create
S|Priority 2 — Intelligence Layer (highest daily value)|### P2-A: Data → dec Layer `Points 2, 3` // - Every anl page gets a "Kai's Read" card: what happened / why it matters / what to do // - Every soc page gets a brand-intelligence card: data → cnt brief → action // - dec cards are NOT optional commentary — they are the primary output // - DRI: Kai (analysis) + Mia (UI card) \| Done when: every metrics page has a "dec to take" section // - **Status:** Scoped // ### P2-B: agt Contradiction + WebSearch `Point 9` // - Marcus, Kai, Rio, Felix gain WebSe
S|Priority 3 — Action & Approval|### P3-A: ap Mechanism in /inbox `Point 8` // - Each dec card in /inbox has: ap / Reject / Defer buttons // - ap logged to Supabase `decisions` table with: agt, decision_text, action_taken, timestamp // - Daily report consolidates: decisions pending your ap + decisions you ap/rejected yesterday // - DRI: Raj (schema + API) + Mia (UI) \| Done when: you can ap a recommendation and it's logged // - **Status:** Scoped // ### P3-B: Daily ses + Report Persistence `Point 10` // - Create Supabase `daily
S|Priority 4 — Content Intelligence|### P4-A: Brand Intelligence pl `Point 2` // - Flow: Kai reads soc stats + cp cnt → auto-generates cnt brief → Lena drafts → Atlas gives direction → you ap // - Triggered daily by Vercel Cron or on-demand from /creative page // - cp cnt fed by existing `/api/cp-cnt` route // - DRI: Kai (brief) + Lena (copy) + Atlas (visual) \| Done when: one-click generates cnt brief from real data // - **Status:** Scoped // ### P4-B: Sidebar Quick Access `Point 5` // - Left sidebar sections: Brands (vtr switche
S|Priority 5 — Autonomy|### P5-A: Self-Developing AI + GitHub Integration `Point 7` // - GitHub API server-side: read repo state, create branch, propose change, open PR // - Dev proposes code changes → shows diff in /tec → Stark approves → Dev pushes // - Error-solve mode: Dev reads build errors, proposes fix, Stark approves, Dev commits // - **Safety gate is non-negotiable:** no auto-push without explicit Stark ap on each change // - DRI: Dev + Raj \| Done when: Dev can propose a code fix and Stark approves/rejects it
S|Priority 6 — Financial Visibility|### P6-A: Weekly API Cost Report `Point 13` // - Felix owns: track Claude API tokens, Apify calls, Supabase reads/writes per week // - Sources: Anthropic usage API, Apify billing API, Supabase dashboard // - Delivered every Monday in Marcus's CEO brief as a cost section // - Alert if weekly spend > budget threshold (set in /settings) // - DRI: Felix + Raj \| Done when: Monday brief includes last week's API costs per service // - **Status:** Scoped // ---
S|Priority 7 — Roadmap View|### P7-A: Stats + Roadmap + Decisions Surface `Point 1` // - `/` Command Center gets a "Roadmap pulse" section: top 3 in-progress items from this file // - Each item shows: state (Scoped / In Flight / Shipped), DRI, last updated // - Decisions feed from Supabase `decisions` table (built in P3-A) // - DRI: Mia + Raj \| Done when: Command Center shows roadmap state + pending decisions // - **Status:** Blocked on P1-A, P3-A // ---
S|Open Decisions (Stark must resolve before build)|\| dec \| Options \| Deadline \| // \|----------\|---------\|----------\| // \| Scout / Personal revival (P4-B) \| Keep killed vs. lightweight tab \| Before P4-B build \| // \| Compact Steve Jobs model (Points 10b/11) \| Philosophy embedded in CLAUDE.md USER.md \| This ses \| // \| RLS in Supabase per vtr \| Add Row Level sec before `daily_logs` table is built \| Before P3-B \| // \| WebSearch provider \| Use existing WebSearch tool in Claude Code vs. Serper API \| Before P2-B build \| // ---
S|Architecture Gaps (must resolve before affected priorities)|\| Gap \| Affects \| Action \| // \|-----\|---------\|--------\| // \| No RLS per vtr in Supabase \| P3-B, P6-A — data could bleed across brands \| Raj to add RLS before new tables \| // \| WebSearch not wired into /api/claude \| P2-B \| Dev to add WebSearch tool to streaming route \| // \| No ap UI \| P3-A \| Mia to build dec card component \| // \| GitHub write access not configured \| P5-A \| Dev to add GitHub token to env, server-side only \| // \| cnt ap exists as verbal rule only \| P4-A \
S|Completed|\| Item \| Date \| Notes \| // \|------\|------\|-------\| // \| **Phase 1 — Data Foundation** \| 2026-04-12 \| 6 new Supabase tables, Stripe webhook, PostHog ingest, cnt Scorer (per-platform weights), Anomaly Alert engine, Audience Momentum, Attribution Map \| // \| **Phase 2 — Brand Pulse** \| 2026-04-12 \| `/brand-pulse` page, top/worst 10 tables, anomaly alert cards, sidebar entry \| // \| **Phase 3 — Market Radar + cg Studio** \| 2026-04-12 \| `cp` + `territory_clusters` tables, scoring eng