#DOC id=PHASE-8-PLAN source=/root/yvon/docs/plans/PHASE-8-PLAN.md format=toon-doc compressed=2026-06-12T06:40:26.342Z

S|# Phase 8 — Technical Debt + Agent Workflow Automation|> **CEO Marcus:** The audits were clean — now we harden. Split the monoliths that make Raj twitch, fix the layout bugs Mia flagged, and start making the agents actually *do* work. // > **Date:** 2026-06-11 // > **Source:** Phase 7 tec audit (Dev/Raj/Mia) + LifeOS agt workflow specs // ---
S|Sprint A — Split the Monoliths|### A1. Split `lib/db.ts` (2,064 lines → domain modules) // Current: one monolithic file with 70+ functions across 40+ tables. // Target: domain-split modules under `lib/db/`: // ``` // lib/db/ //   index.ts        → re-exports everything (backward compat) //   ventures.ts     → vtr CRUD, socials, dpl //   tasks.ts        → tasks, deliverables, decisions //   cnt.ts      → content_calendar, content_suggestions, content_series, pitches //   agents.ts       → agt sessions, mem, settings, skills //
S|Sprint B — E2E Testing Foundation|### B1. Playwright test suite // - Smoke tests: all 33 pages load without crashing // - Critical path tests: vtr switching, settings save, dpl connect // - Visual regression baselines (optional — Phase 8B) // ### B2. CI gating // - Add Playwright job to CI pl // - Block deploy on test failure // - Require tsc + lint + test before merge // ---
S|Sprint C — Agent Workflow Automation (Phase 1)|### C1. Henry — dec Queue Filtering // - Cron job: hourly scan of all pending items across ventures // - Filter: auto-handle safe items, escalate only items needing human dec // - Learning: track which items owner approves/rejects → adjust filter over time // ### C2. Nexus — PR-Only Coding // - GitHub integration: Nexus can open PRs but merge requires owner ap // - Software pl: tasks auto-flow TRIAGE → PLANNING → IN PROGRESS (Nexus) → STEVE QA // ### C3. Steve — QA Gate // - After Nexus opens PR
S|Quality Gates (unchanged)|- tsc --noEmit: 0 errors // - ESLint: 0 new warnings // - next build: 33 pages compiled // - Push to GitHub → Vercel auto-deploy