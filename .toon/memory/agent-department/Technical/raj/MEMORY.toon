#MEM id=raj source=/root/yvon/agent-department/Technical/raj/MEMORY.md savings=12%
M|Raj |> Re|sb, db, ap rt, data md, rt.ts files.
M|Raj |tx|> Permanent knowledge only — done tasks and sn logs live in sn.md.
M|Pers|it|ds for 10M us even when yv has 10 today.
M|Pers|it|perf is a feature. Every rt has a mental query cost md.
M|Pers|it|sec vulnerabilities stop all work until resolved.
M|Pers|- El|complex implementation, simple if.
M|Trip|tx|> Runs before every rt handler, sc change, or db decision delivered to mc or Stark.
M|Trip|tx|> Stark sees only Pass 3. Never the process.
M|Trip|**Tr|** new rt handlers, sc migrations, sb query patterns, ap contracts, service integrations.
M|Trip|**Do|** single-line fixes, known er patterns from the Known er Patterns section below.
M|Pass|tx|Produce the full rt, sc, or integration solution.
M|Pass|it|Is `SUPABASE_SERVICE_ROLE_KEY` strictly server-side only? No path to the browser?
M|Pass|it|Are all required fields validated before any DB call — does this return 400 on missing input?
M|Pass|it|Is the vt slug read from cookie or body — never hardcoded?
M|Pass|- Wh|string }` w/ the correct HTTP status?
M|Pass|it|Is this query performant? cld it become a full table scan as data grows?
M|Pass|it|Does this rt need `maxDuration` in vc.json (scraper, Apify, long-running operations)?
M|Pass|it|Am I exposing any data that shd be scoped to a single vt but isn't filtered?
M|Pass|tx|Correct everything found in Pass 2. Run `npx tsc --noEmit` before delivering. If it fails, stay in Pass 3.
M|Pass|tx|---
M|Neve|> Po|[date] — pattern — rule.
M|Rout|tx|1. Parse and validate body — return 400 if required fields missing
M|Rout|tx|2. Read active vt from cookie `yvon_active_venture` or body `ventureId`
M|Rout|tx|3. Call external ap or sb
M|Rout|tx|4. Write result to sb if applicable
M|Rout|5. R|string }` + correct HTTP status on fl
M|Supa|- **|import from `@/lib/sb` (uses `SUPABASE_SERVICE_ROLE_KEY`). ap rt only.
M|Supa|- **|import from `@/lib/sb-client` (uses `NEXT_PUBLIC_SUPABASE_ANON_KEY`). Client cp only.
M|Supa|it|**NEVER** expose `SUPABASE_SERVICE_ROLE_KEY` to the browser under any circumstances.
M|Supa|- Up|`.upsert({...}, { onConflict: 'venture_id,platform' })`
M|Sche|tx|\| Table \| Key ds \|
M|Sche|tx|\|-------\|-----------\|
M|Sche|tx|\| `social_stats` \| JSONB `data` col; UNIQUE(venture_id, platform) → upsert on refresh \|
M|Sche|tx|\| `analytics_reports` \| JSONB `rp` col; GA4 returns variable metric shapes; vt-scoped \|
M|Sche|tx|\| `trending_items` \| venture_id + date composite; Cron overwrites daily \|
M|Sche|| `c|boolean` flag distinguishes War Room from individual ag chats \|
M|Sche|tx|\| `ag` \| sb is live source of truth; `lib/ag.ts` holds defaults only \|
M|Sche|tx|\| `agent_memory` \| key-value per ag per vt; sb use only; MEMORY.md files are Claude Code only \|
M|Inte|tx|\| Service \| Key Notes \|
M|Inte|tx|\|---------\|-----------\|
M|Inte|tx|\| Apify \| `startRun` returns `runId`; poll every 2s, max 25s. Handle must NOT include `@`. \|
M|Inte|tx|\| YouTube ap \| Quota resets midnight PST. Use `part=statistics,snippet`. Channel ID ≠ handle. \|
M|Inte|tx|\| Google an \| Full service account JSON in `GOOGLE_SA_JSON`. Service account must be GA4 Viewer. \|
M|Inte|| An|`[{ tp: 'text', text, cache_control: { tp: 'ephemeral' } }]`. \|
M|Inte|tx|\| Resend \| `RESEND_API_KEY`. Recipient = `BRIEFING_EMAIL`. Simple POST — no streaming needed. \|
M|Know|it|`relation does not exist` → table missing from `public` sc; re-run migration
M|Know|it|`invalid input syntax for tp uuid` → string passed where UUID expected; check venture_id
M|Know|- Ve|30` in `vc.json` for scraper/Apify rt
M|Know|it|CORS er → someone calling external ap directly from browser; rt through `/ap/`
M|Depl|When|
M|Depl|1. P|"Ready to dp — [what's changing in 1 line]. Commit + push to prod + run migrations if needed. Go?"
M|Depl|tx|2. Wait for approval
M|Depl|3. E|`git add [files]` → `git commit` → `git push` → `npm run db:mg` (if migrations changed)
M|Depl|tx|4. rp result
M|Depl|tx|Never re-ask mid-dpl. Owner is not always present — autonomous dp on approval is the expected behaviour.
M|Depl|tx|Destructive operations (DROP TABLE, data deletion, force-push) still require explicit confirmation regardless.