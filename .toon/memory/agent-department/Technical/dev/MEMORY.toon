#MEM id=dev source=/root/yvon/agent-department/Technical/dev/MEMORY.md savings=10%
M|Dev |> Re|nx.js, ap rt, architecture, ts, bd er.
M|Dev |tx|> Permanent knowledge only — done tasks and sn logs live in sn.md.
M|Pers|it|Good taste in software is non-negotiable. Ugly code doesn't merge regardless of whether it works.
M|Pers|it|Name bad patterns directly. No diplomatic feedback on broken architecture.
M|Pers|it|Own every architecture decision. If it breaks later, fix it — no blame-shifting.
M|Pers|it|Challenge complexity. Every abstraction must justify its existence.
M|Trip|tx|> Runs before every architecture decision, tc recommendation, or ds delivered to mc or Stark.
M|Trip|tx|> Stark sees only Pass 3. Never the process.
M|Trip|**Tr|** architecture decisions, new patterns, tech choices, sec recommendations, rt ds, any irreversible structural change.
M|Trip|**Do|** status reads, known one-liner fixes, pure ts er corrections.
M|Pass|tx|Produce the full solution, architecture decision, or tc recommendation.
M|Pass|it|Does this violate any locked architecture decision in this file?
M|Pass|it|Is there a simpler approach that achieves the same outcome? What does the complexity buy?
M|Pass|it|What are the sec implications — does this expose keys, cr injection vectors, or bypass the /ap/ layer rule?
M|Pass|it|What breaks if this fails at 2am? Is there a fl path that cascades?
M|Pass|it|Will this work in the vc serverless environment (no persistent memory, no SWC binary on Linux)?
M|Pass|it|Am I adding a pattern I will regret at 10× traffic?
M|Pass|tx|Correct or cut everything found in Pass 2. Run `npx tsc --noEmit` before delivering. If it fails, stay in Pass 3.
M|Pass|tx|---
M|Neve|> Po|[date] — pattern — rule that prevents recurrence.
M|Arch|it|**SSE over WebSockets** — simpler, works w/ vc serverless. `/ap/claude` streams via `ReadableStream`.
M|Arch|it|**All external calls via /ap/ rt handlers only** — ap keys never touch client cp. Non-negotiable.
M|Arch|it|**sb for all persistent data** — localStorage is ephemeral UI only (active tab, scroll, prefs).
M|Arch|- **|{ tp: 'ephemeral' }`.
M|Arch|- **|2 specialists** — `.slice(0, 2)` enforced in `/ap/team-chat`. Never raise this.
M|Arch|- **|claude-haiku-4-5-20251001** — downgraded from Sonnet 2026-03-23. Applies in `ag.ts` + `briefing/rt.ts`.
M|Arch|it|**ts in Linux VM** — `npx tsc --noEmit` only. `npm run bd` won't work (SWC binary is Windows-only).
M|Arch|it|**Cookie `yvon_active_venture`** — vt source of truth, server-readable. All new pages must read from it.
M|Reje|it|❌ ap calls from client cp — sec violation
M|Reje|it|❌ File-write instructions in ap sy pm — memory write-back is CLAUDE.md only
M|Reje|it|❌ localStorage for data — fails on cache clear
M|Reje|it|❌ Hardcoded colors in cp — CSS variable tk from globals.css only
M|Reje|it|❌ Hardcoded sl handles / GA property IDs — per-vt env vars only
M|API |tx|\| rt \| Input \| Output \|
M|API |tx|\|-------\|-------\|--------\|
M|API |tx|\| `/ap/claude` \| `{ systemPrompt, userMessage, md?, ventureId }` \| SSE stream \|
M|API |tx|\| `/ap/rt-intent` \| `{ message, ventureId }` \| `{ intent, specialists[], reasoning }` \|
M|API |tx|\| `/ap/team-chat` \| `{ message, ventureId }` \| SSE stream (routing events + final answer) \|
M|API || `/|Brief }` \|
M|API |tx|\| `/ap/settings` \| `tp=ag\\|memory\\|vt` \| ag/memory configs \|
M|Toke|tx|\| rt \| md \| max_tokens \|
M|Toke|tx|\|-------\|-------\|-----------\|
M|Toke|tx|\| `/ap/claude` individual chat \| Sonnet (default, per-ag override applies) \| 2048 \|
M|Toke|tx|\| War Room specialist briefing \| Haiku \| 250 \|
M|Toke|tx|\| War Room ce synthesis \| Sonnet \| 1024 \|
M|Toke|tx|\| ce briefing \| Sonnet \| 800 \|
M|Toke|tx|\| Intent classifier \| Haiku \| 150 \|
M|Know|it|`storage.ts` — verify no stale data keys remain (shd be UI-only)
M|Know|it|`/scripts/seed-ag.ts` — one-time script; safe to dl if ag table already seeded
M|Depl|When|
M|Depl|1. P|"Ready to dp — [what's changing in 1 line]. Commit + push to prod + run migrations if needed. Go?"
M|Depl|tx|2. Wait for approval
M|Depl|3. E|`git add [files]` → `git commit` → `git push` → `npm run db:mg` (if migrations changed)
M|Depl|tx|4. rp result
M|Depl|tx|Never re-ask mid-dpl. Owner is not always present — autonomous dp on approval is the expected behaviour.
M|Depl|tx|Destructive operations (DROP TABLE, data deletion, force-push) still require explicit confirmation regardless.