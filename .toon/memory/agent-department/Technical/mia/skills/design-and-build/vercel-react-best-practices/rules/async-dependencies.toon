#MEM id=rules source=/root/yvon/agent-department/Technical/mia/skills/design-and-build/vercel-react-best-practices/rules/async-dependencies.md
M|rules|title: Dependency-Based Parallelization
M|rules|impact: CRITICAL
M|rules|impactDescription: 2-10× improvement
M|rules|tags: async, parallelization, dependencies, better-all
M|rules|For operations with partial dependencies, use `better-all` to maximize parallelism. It automatically starts each task at the earliest possible moment.
M|rules|**Incorrect (profile waits for config unnecessarily):**
M|rules|```typescript
M|rules|const [user, config] = await Promise.all([
M|rules|fetchUser(),
M|rules|fetchConfig()
M|rules|])
M|rules|const profile = await fetchProfile(user.id)
M|rules|```
M|rules|**Correct (config and profile run in parallel):**
M|rules|```typescript
M|rules|import { all } from 'better-all'
M|rules|const { user, config, profile } = await all({
M|rules|async user() { return fetchUser() },
M|rules|async config() { return fetchConfig() },
M|rules|async profile() {
M|rules|return fetchProfile((await this.$.user).id)
M|rules|}
M|rules|})
M|rules|```
M|rules|**Alternative without extra dependencies:**
M|rules|We can also create all the promises first, and do `Promise.all()` at the end.
M|rules|```typescript
M|rules|const userPromise = fetchUser()
M|rules|const profilePromise = userPromise.then(user => fetchProfile(user.id))
M|rules|const [user, config, profile] = await Promise.all([
M|rules|userPromise,
M|rules|fetchConfig(),
M|rules|profilePromise
M|rules|])
M|rules|```
M|rules|Reference: [https://github.com/shuding/better-all](https://github.com/shuding/better-all)