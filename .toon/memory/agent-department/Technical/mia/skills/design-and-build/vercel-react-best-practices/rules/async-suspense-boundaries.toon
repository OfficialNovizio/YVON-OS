SRC agent-department/Technical/mia/skills/design-and-build/vercel-react-best-practices/rules/async-suspense-boundaries.md
ORIG_SIZE 2510
SECTIONS 16
ABBREVS 2
INDEX_TERMS 92
COMPILED 2026-06-13T19:23:25.443Z
---
TITLE Strategic Suspense Boundaries
STRATEGIC SUSPENSE BOUNDARIES
IO awaiting data in async components before returning JSX, use Suspense boundaries to show the wrapper UI faster while data loads. 
Incorrect (wrapper blocked by data fetching): 
CODE tsx 254CHARS
The entire layout waits for data even though OT middle section needs it. 
Correct (wrapper shows immediately, data streams in): 
CODE tsx 375CHARS
Sidebar, Header, and Footer render immediately. Only DataDisplay waits for data. 
Alternative (share promise across components): 
CODE tsx 705CHARS
Both components share the same promise, so only one fetch occurs. Layout renders immediately while both components wait together. 
When NOT to use this pattern: 
Critical data needed for layout decisions (affects positioning) SEO-critical content above the fold Small, fast queries where suspense overhead isn't worth it When you want to avoid layout shift (loading → content jump) 
Trade-off: Faster initial paint vs potential layout shift. Choose based on your UX priorities. 