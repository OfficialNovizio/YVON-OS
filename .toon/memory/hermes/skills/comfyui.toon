SKILL|comfyui|"Generate images, video, and audio with ComfyUI — install, launch, manage nodes/models, run workflows with parameter injection. Uses the official comfy-cli for lifecycle and direct REST/WebSocket API for execution."
STEP|1|Reference docs (`references/`):**
STEP|2|`official-cli.md` — every `comfy ...` command, with flags
STEP|3|`rest-api.md` — REST + WebSocket endpoints (local + cloud), payload schemas
STEP|4|`workflow-format.md` — API-format JSON, common node types, param mapping
STEP|5|`template-integrity.md` — converting `comfyui-workflow-templates` from
STEP|6|Scripts (`scripts/`):**
STEP|7|Example workflows (`workflows/`):** SD 1.5, SDXL, Flux Dev, SDXL img2img,
STEP|8|User asks to generate images with Stable Diffusion, SDXL, Flux, SD3, etc.
STEP|9|User wants to run a specific ComfyUI workflow file
STEP|10|User wants to chain generative steps (txt2img → upscale → face restore)
STEP|11|User needs ControlNet, inpainting, img2img, or other advanced pipelines
STEP|12|User asks to manage ComfyUI queue, check models, or install custom nodes
STEP|13|User wants video/audio/3D generation via AnimateDiff, Hunyuan, Wan, AudioCraft, etc.
STEP|14|Why two layers?** The official CLI is excellent for installation and server
STEP|15|ComfyUI web UI → **Workflow → Export (API)** (newer UI) or
STEP|16|This skill's `workflows/` directory (ready-to-run examples)
STEP|17|Community downloads (civitai, Reddit, Discord) — usually editor format,
STEP|18|Official docs:** https://docs.comfy.org/installation
STEP|19|CLI docs:** https://docs.comfy.org/comfy-cli/getting-started
STEP|20|Cloud docs:** https://docs.comfy.org/get_started/cloud
STEP|21|Cloud API:** https://docs.comfy.org/development/cloud/overview
STEP|22|**Cloud** → skip to **Path A**.
STEP|23|**Local** → run hardware check first, then pick a path from Paths B–E based on the verdict.
STEP|24|**Unsure** → run the hardware check and let the verdict decide.
STEP|25|Docs:** https://docs.comfy.org/get_started/cloud
STEP|26|Sign up at https://comfy.org/cloud
STEP|27|Generate an API key at https://platform.comfy.org/login
STEP|28|Set the key:
STEP|29|Run workflows:
STEP|30|Pricing:** https://www.comfy.org/cloud/pricing
STEP|31|Concurrent jobs:** Free/Standard 1, Creator 3, Pro 5. Free tier
STEP|32|cannot run workflows via API** — only browse models. Paid subscription
STEP|33|Docs:** https://docs.comfy.org/installation/desktop
STEP|34|**Windows (NVIDIA):** https://download.comfy.org/windows/nsis/x64
STEP|35|**macOS (Apple Silicon):** https://comfy.org
STEP|36|Docs:** https://docs.comfy.org/installation/comfyui_portable_windows
STEP|37|Docs:** https://docs.comfy.org/comfy-cli/getting-started
STEP|38|Docs:** https://docs.comfy.org/installation/manual_install
STEP|39|**Base URL:** `https://cloud.comfy.org`
STEP|40|**Auth:** `X-API-Key` header (or `?token=KEY` for WebSocket)
STEP|41|**API key:** set `$COMFY_CLOUD_API_KEY` once and the scripts pick it up automatically
STEP|42|**Output download:** `/api/view` returns a 302 to a signed URL; the scripts
STEP|43|**Endpoint differences from local ComfyUI:**
STEP|44|**Concurrent jobs:** Free/Standard: 1, Creator: 3, Pro: 5. Extras queue
STEP|45|**API format required** — every script and the `/api/prompt` endpoint expect
STEP|46|**Server must be running** — all execution requires a live server.
STEP|47|**Model names are exact** — case-sensitive, includes file extension.
STEP|48|**Missing custom nodes** — "class_type not found" means a required node
STEP|49|**Working directory** — `comfy-cli` auto-detects the ComfyUI workspace.
STEP|50|**Cloud free-tier API limits** — `/api/prompt`, `/api/view`, `/api/upload/*`,
STEP|51|**Timeout for video/audio workflows** — auto-detected when an output node
STEP|52|**Path traversal in output filenames** — server-supplied filenames are
STEP|53|**Workflow JSON is arbitrary code** — custom nodes run Python, so
STEP|54|**Auto-randomized seed** — pass `seed: -1` in `--args` (or use
STEP|55|**`tracking` prompt** — first run of `comfy` may prompt for analytics.
STEP|56|[ ] `hardware_check.py` verdict is `ok` OR the user explicitly chose Comfy Cloud
STEP|57|[ ] `comfy --version` works (or `uvx --from comfy-cli comfy --help`)
STEP|58|[ ] `curl http://HOST:PORT/system_stats` returns JSON
STEP|59|[ ] `comfy model list` shows at least one checkpoint (local) OR
STEP|60|[ ] Workflow JSON is in API format
STEP|61|[ ] `check_deps.py` reports `is_ready: true` (or only `node_check_skipped`
STEP|62|[ ] Test run with a small workflow completes; outputs land in `--output-dir`
PIT|**API format required** — every script and the `/api/prompt` endpoint expect
PIT|**Server must be running** — all execution requires a live server.
PIT|**Model names are exact** — case-sensitive, includes file extension.
PIT|**Missing custom nodes** — "class_type not found" means a required node
PIT|**Working directory** — `comfy-cli` auto-detects the ComfyUI workspace.
PIT|**Cloud free-tier API limits** — `/api/prompt`, `/api/view`, `/api/upload/*`,
PIT|**Timeout for video/audio workflows** — auto-detected when an output node
PIT|**Path traversal in output filenames** — server-supplied filenames are
PIT|**Workflow JSON is arbitrary code** — custom nodes run Python, so
PIT|**Auto-randomized seed** — pass `seed: -1` in `--args` (or use
PIT|**`tracking` prompt** — first run of `comfy` may prompt for analytics.