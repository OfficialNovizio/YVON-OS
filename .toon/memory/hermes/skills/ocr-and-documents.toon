SKILL|ocr-and-documents|"Extract text from PDFs/scans (pymupdf, marker-pdf)."
STEP|1|Decision**: Use pymupdf unless you need OCR, equations, forms, or complex layout analysis.
STEP|2|Via helper script**:
STEP|3|Inline**:
STEP|4|Via helper script**:
STEP|5|CLI** (installed with marker-pdf):
STEP|6|`web_extract` is always first choice for URLs
STEP|7|pymupdf is the safe default — instant, no models, works everywhere
STEP|8|marker-pdf is for OCR, scanned docs, equations, complex layouts — install only when needed
STEP|9|Both helper scripts accept `--help` for full usage
STEP|10|marker-pdf downloads ~2.5GB of models to `~/.cache/huggingface/` on first use
STEP|11|For Word docs: `pip install python-docx` (better than OCR — parses actual structure)
STEP|12|For PowerPoint: see the `powerpoint` skill (uses python-pptx)
PIT|`web_extract` is always first choice for URLs
PIT|pymupdf is the safe default — instant, no models, works everywhere
PIT|marker-pdf is for OCR, scanned docs, equations, complex layouts — install only when needed
PIT|Both helper scripts accept `--help` for full usage
PIT|marker-pdf downloads ~2.5GB of models to `~/.cache/huggingface/` on first use
PIT|For Word docs: `pip install python-docx` (better than OCR — parses actual structure)
PIT|For PowerPoint: see the `powerpoint` skill (uses python-pptx)