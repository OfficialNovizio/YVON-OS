SKILL|simplify-code|"Parallel 3-agent cleanup of recent code changes."
STEP|1|Core principle:** Three narrow reviewers beat one broad reviewer. Each one
STEP|2|"simplify" / "simplify my changes" / "simplify these changes"
STEP|3|"review my code" / "review my recent changes" / "clean up my changes"
STEP|4|"/simplify" (if they're carrying the Claude Code habit over)
STEP|5|**Focus:** "simplify focus on efficiency" → run only the efficiency reviewer
STEP|6|**Dry run:** "simplify but don't change anything" / "just report" → run the
STEP|7|**Scope:** "simplify the last commit" / "simplify staged" / "simplify
STEP|8|Search the existing codebase for evidence (don't reason from the diff alone).
STEP|9|Report findings as a concrete list: `file:line → problem → suggested fix`.
STEP|10|Rank each finding `high` / `medium` / `low` confidence.
STEP|11|Skip nits and style-only churn. Only flag things that materially improve
STEP|12|Reviewer 1 — Code Reuse**
STEP|13|Reviewer 2 — Code Quality**
STEP|14|Reviewer 3 — Efficiency**
STEP|15|**Merge** the findings into one list, deduping where reviewers overlap.
STEP|16|**Discard false positives** — you have the most context; you don't have to
STEP|17|**Resolve conflicts.** Reviewers can disagree (Reviewer 1: "use existing
STEP|18|**Apply** the surviving fixes directly with `patch` / `write_file` — unless
STEP|19|**Verify** you didn't break anything: run the project's targeted tests for
STEP|20|**Summarize** what you changed: a short list of applied fixes grouped by
STEP|21|**Don't fan out wider than ~3.** More reviewers means more cost and more
STEP|22|**Give the WHOLE diff to each reviewer.** Splitting the diff across reviewers
STEP|23|**Reviewers search, they don't guess.** A reuse finding with no pointer to
STEP|24|**Apply ≠ rewrite.** This is cleanup of the user's recent changes, not a
STEP|25|**Respect project conventions.** If the repo has AGENTS.md / CLAUDE.md /
STEP|26|**Large diffs blow context.** If the diff is huge, scope it down before
PIT|**Don't fan out wider than ~3.** More reviewers means more cost and more
PIT|**Give the WHOLE diff to each reviewer.** Splitting the diff across reviewers
PIT|**Reviewers search, they don't guess.** A reuse finding with no pointer to
PIT|**Apply ≠ rewrite.** This is cleanup of the user's recent changes, not a
PIT|**Respect project conventions.** If the repo has AGENTS.md / CLAUDE.md /
PIT|**Large diffs blow context.** If the diff is huge, scope it down before